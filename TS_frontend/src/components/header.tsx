import Head from "next/head";

const HeadComponent = (): JSX.Element => (
  <Head>
    <title>Plant</title>
    <meta name="Aoi Plan Managament" content="Planting" />
    <link rel="icon" href="/h-log.png" />
  </Head>
);

export default HeadComponent;
